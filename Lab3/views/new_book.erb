<div class="grid-x grid-padding-x">
  <div class="large-12 cell">
    <h3>Добавление прочитанной книги</h3>
  </div>
  <% if @params && !@params[:error].empty? %>
    <div class="large-12 cell callout">
      <p>При заполнении формы возникли ошибки</p>
      <ul>
        <% @params[:error].each do |error| %>
          <li><%= error %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="large-12 cell">
    <form method="post">
      <div class="grid-container">
        <div class="grid-x grid-padding-x">
          <div class="large-12 cell">
            <label>Автор произведения
              <input type="text" placeholder="Рей Бредбери" name="author" value="<%= @params && @params[:author] %>">
            </label>
          </div>
          <div class="large-12 cell">
            <label>Название произведения
              <input type="text" placeholder="Вино из одуванчиков" name="title" value="<%= @params && @params[:title] %>">
            </label>
          </div>
          <div class="large-12 cell">
            <label>Дата прочтения
              <input type="text" placeholder="2020-14-05" name="date" value="<%= @params && @params[:date] %>">
            </label>
          </div>
          <div class="large-12 cell">
            <label>Формат чтения
              <input type="text" placeholder="Бумажная книга" name="formats" value="<%= @params && @params[:formats] %>">
            </label>
          </div>
          <div class="large-12 cell">
            <label>Размер произведения
              <input type="text" placeholder="12" name="size" value="<%= @params && @params[:size] %>">
            </label>
          </div>
           <div class="large-12 cell">
            <label>Оценка
              <input type="text" placeholder="5" name="point" value="<%= @params && @params[:point] %>">
            </label>
          </div>
          <div class="large-12 cell">
            <label>Необязательный текстовый комментарий
              <input type="text" placeholder="Не обязательно" name="txt" value="<%= @params && @params[:txt] %>">
            </label>
          </div>
          <div class="large-12 cell">
            <input type="submit" value="Сохранить" class="button">
          </div>
        </div>
      </div>
    </form>
  </div>
</div>